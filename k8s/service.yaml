apiVersion: v1
kind: Service
metadata:
  labels:
    app: taskapi
  name: taskapi
spec:
  ports:
  - name: 80-3000
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: taskapi
  type: ClusterIP
status:
  loadBalancer: {}
